function handleManualCommand(text, jid, isOwner, pushName) {
    const cmd = text.trim();

    // نظام كلمة السر للدخول للوحة التحكم من أي رقم
    if (cmd === "نجم1997") {
        return `*اهلا بك في لوحة التحكم المركزية* 🛠
--------------------------
مرحباً يا ${pushName}، ماذا تريد أن أفعل؟

1- *حاله* : لفحص اتصال البوت.
2- *الاحصائيات* : لمعرفة عدد المحادثات.
3- *المجموعات* : قائمة المجموعات المشتركة.
4- *توقف* : إيقاف الرد الآلي مؤقتاً.
5- *تشغيل* : استئناف العمل.

_أرسل الكلمة المطلوبة فقط_`;
    }

    // الأوامر داخل لوحة التحكم
    if (cmd === "حاله") return "الوضع: متصل ✅\nالسرعة: ممتازة 🚀\nالجهاز: جوال (Android)";
    
    if (cmd === "الاحصائيات") {
        return `سجل العمليات:\n• المستخدمين النشطين: ${Object.keys(jid).length}\n• الرسائل المعالجة: مستقرة`;
    }

    if (isOwner && cmd === "توقف") return "🔴 تم إيقاف الرد الآلي. أنا في وضع الاستعداد لأوامرك فقط.";
    if (isOwner && cmd === "تشغيل") return "🟢 تم تفعيل الرد الآلي والذكاء الاصطناعي.";

    return null;
}

module.exports = { handleManualCommand };
