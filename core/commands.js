function handleManualCommand(text, jid, isOwner, pushName) {
    const cmd = text.trim();

    if (cmd === "نجم1997") {
        return `*اهلا بك في لوحة التحكم المركزية* 🛠
--------------------------
مرحباً يا ${pushName}، ماذا تريد أن أفعل؟

1- *حاله* : لفحص الاتصال.
2- *الاحصائيات* : عدد المحادثات.
3- *المجموعات* : القائمة.
4- *توقف* : إيقاف الرد الآلي.
5- *تشغيل* : استئناف العمل.

_أرسل الرقم (1-5) أو الكلمة_`;
    }

    // دعم الأرقام والكلمات معاً
    if (cmd === "حاله" || cmd === "1") return "الوضع: متصل ✅\nالجهاز: Android (Mobile Control)";
    if (cmd === "الاحصائيات" || cmd === "2") return `النشاط: مستقر 🚀\nالمستخدم: ${pushName}`;
    if (isOwner && (cmd === "توقف" || cmd === "4")) return "🔴 أبشر، أوقفت الرد الآلي. سأكتفي بتنفيذ أوامرك اليدوية فقط.";
    if (isOwner && (cmd === "تشغيل" || cmd === "5")) return "🟢 تم تفعيل الذكاء الاصطناعي والرد البشري مجدداً.";

    return null;
}
module.exports = { handleManualCommand };
